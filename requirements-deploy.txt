# Core dependencies with fixed versions
wheel==0.41.2
setuptools==69.0.3
pip==25.1.1

# Base packages (faster to install)
streamlit==1.31.0
numpy==1.24.3
pandas==2.0.3
matplotlib==3.7.2
seaborn==0.12.2
plotly==5.13.1

# NLP dependencies (in order)
nltk==3.8.1
textblob==0.17.1
emoji==2.2.0
urlextract==1.3.0

# Word cloud dependencies
wordcloud==1.9.2

# Machine learning (with fixed versions for better compatibility)
scikit-learn==1.3.0

# Heavier packages with known wheels
# These are placed last to ensure dependencies are resolved first
gensim==4.3.1
spacy==3.7.2

# Pre-included spaCy language model to avoid download during deployment
https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.7.0/en_core_web_sm-3.7.0-py3-none-any.whl

# Version-fixed markdown dependencies to avoid conflicts
markdown-it-py==2.2.0
mdurl==0.1.2
pygments==2.15.1
rich==13.4.2
